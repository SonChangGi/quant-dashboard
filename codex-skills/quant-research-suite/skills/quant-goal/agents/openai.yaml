interface:
  display_name: "Quant Goal"
  short_description: "Start or steer a native Goal with $quant-goal"
  default_prompt: "Use $quant-goal to bind or resume one native Goal, pursue it with selective delegation and free-only fallbacks, and transition it only on verified completion or a genuine repeated blocker."
policy:
  allow_implicit_invocation: false
