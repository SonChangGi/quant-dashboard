interface:
  display_name: "Quant Developer"
  short_description: "Manual end-to-end Quant implementation"
  default_prompt: "Use $quant-developer only when explicitly invoked to deliver the complete accepted outcome with minimal unrelated churn, adapt across available tools and free data sources, use independent workers selectively, verify the actual surface, and respect separate authority boundaries."
policy:
  allow_implicit_invocation: false
