interface:
  display_name: "Quant Developer"
  short_description: "Adaptive end-to-end Quant delivery"
  default_prompt: "Use $quant-developer to deliver the accepted outcome with capability-aware delegation, zero-billing data routes when needed, and verification on the actual consumer surface."
policy:
  allow_implicit_invocation: false
