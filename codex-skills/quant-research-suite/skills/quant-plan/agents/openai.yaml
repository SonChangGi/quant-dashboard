interface:
  display_name: "Quant Plan"
  short_description: "Manual read-only planning and audits"
  default_prompt: "Use $quant-plan to inspect the target read-only and return an audit, quick plan, or decision-complete implementation plan with explicit assumptions and observable acceptance."
policy:
  allow_implicit_invocation: false
