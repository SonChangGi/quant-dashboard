interface:
  display_name: "Quant Plan"
  short_description: "Manual adaptive read-only Quant planning"
  default_prompt: "Use $quant-plan to inspect the target read-only, scale depth and capability guidance to the task, and return the smallest sufficient audit, quick plan, or decision-complete implementation plan with observable acceptance."
policy:
  allow_implicit_invocation: false
